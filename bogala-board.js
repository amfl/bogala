export default class Board extends Array {
    constructor(boardSpec) { super(); }

    getTileSymbol(canvas) { }

    forEach(...args) { return super.forEach(...args) }

    getTile(x, y) { }
    viewbox() { }
}
